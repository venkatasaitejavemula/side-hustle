# ---------------------------------------------------------------------------
# PostgreSQL (used when POSTGRES_HOST is set; otherwise app uses SQLite)
# ---------------------------------------------------------------------------
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=predictor
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=predictions

# ---------------------------------------------------------------------------
# Email (Gmail: enable 2FA, then create app password at https://myaccount.google.com/apppasswords)
# Leave empty to print reports to console instead of sending email.
# ---------------------------------------------------------------------------
EMAIL_SENDER=work.with.tymly@gmail.com
EMAIL_PASSWORD=kelf zoic axba jyjd
EMAIL_RECIPIENT=saiteja8906@gmail.com
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
